name: fleetiq_driver_app
description: FleetIQ Flutter driver client with offline action synchronization
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.3

flutter:
  uses-material-design: true
